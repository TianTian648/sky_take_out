<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into dish (category_id, description, image, name, price, status, create_time, update_time, create_user,
                          update_user)
        VALUES (#{categoryId}, #{description}, #{image}, #{name}, #{price}, #{status}, #{createTime}, #{updateTime},
                #{createUser}, #{updateUser})
    </insert>
    <select id="pageQuery" resultType="com.sky.entity.Dish">
        select * from dish
        <where>
            <if test="categoryId != '' and categoryId != null">category_id = #{categoryId}</if>
            <if test="name != null and name != ''">and name = #{name}</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
    </select>
</mapper>